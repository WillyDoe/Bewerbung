<UserControl x:Class="tei_penService_ui.Controls.FormularTextInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock 
            x:Name="LabelTextBlock"
            Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Margin="0,0,0,4"/>
        <Border 
            x:Name="TextInputBorder"
            BorderBrush="{DynamicResource HellgrauBrush}" 
            BorderThickness="1"
            CornerRadius="6"
            Background="Transparent"
            ClipToBounds="True">
            <TextBox x:Name="TextInputTextBox"
                    Background="Transparent"
                     Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Foreground="{DynamicResource TextBrush}"
                     GotFocus="TextInputTextBox_GotFocus"
                     LostFocus="TextInputTextBox_LostFocus"
                     FontSize="14"
                     Padding="12,10"
                     BorderThickness="0"/>
        </Border>
    </StackPanel>
</UserControl>
