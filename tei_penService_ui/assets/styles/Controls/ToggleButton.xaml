<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- LoginTabToggleStyle - für Anmelden/Registrieren Tabs -->
    <Style x:Key="LoginTabToggleStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource TeiBluePastelSanftBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource TeiBluePastelSanftBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================ -->
    <!-- Toggle Switch Styles - Apple-like -->
    <!-- ============================================ -->
    
    <!-- DarkModeToggleSwitchStyle - Moderner Apple-ähnlicher Toggle (20% schlanker) -->
    <Style x:Key="DarkModeToggleSwitchStyle" TargetType="ToggleButton">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="19"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="OnAnimation">
                            <ColorAnimation Storyboard.TargetName="TrackBorder"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="#E8E8E8"
                                            Duration="0:0:0.2">
                                <ColorAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut"/>
                                </ColorAnimation.EasingFunction>
                            </ColorAnimation>
                            <ColorAnimation Storyboard.TargetName="Thumb"
                                            Storyboard.TargetProperty="(Ellipse.Fill).(SolidColorBrush.Color)"
                                            To="#2D2D2D"
                                            Duration="0:0:0.2">
                                <ColorAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut"/>
                                </ColorAnimation.EasingFunction>
                            </ColorAnimation>
                            <DoubleAnimation Storyboard.TargetName="ThumbTranslate"
                                             Storyboard.TargetProperty="X"
                                             To="12"
                                             Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                        <Storyboard x:Key="OffAnimation">
                            <ColorAnimation Storyboard.TargetName="TrackBorder"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="#FFBF20"
                                            Duration="0:0:0.2">
                                <ColorAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut"/>
                                </ColorAnimation.EasingFunction>
                            </ColorAnimation>
                            <ColorAnimation Storyboard.TargetName="Thumb"
                                            Storyboard.TargetProperty="(Ellipse.Fill).(SolidColorBrush.Color)"
                                            To="#E0E0E0"
                                            Duration="0:0:0.2">
                                <ColorAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut"/>
                                </ColorAnimation.EasingFunction>
                            </ColorAnimation>
                            <DoubleAnimation Storyboard.TargetName="ThumbTranslate"
                                             Storyboard.TargetProperty="X"
                                             To="0"
                                             Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseInOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Grid>
                        <!-- Track (Hintergrund) -->
                        <Border x:Name="TrackBorder"
                                Width="32"
                                Height="19"
                                CornerRadius="10"
                                Background="#FFBF20">
                            <Grid>
                                <!-- WPF-System-Icons: Segoe MDL2 Assets (E708=QuietHours/Mond, E706=Brightness/Sonne) -->
                                <TextBlock x:Name="MoonIcon"
                                          Text="&#xE708;"
                                          FontFamily="Segoe MDL2 Assets"
                                          FontSize="10"
                                          Foreground="#2D2D2D"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          Margin="0,0,5,0"
                                          Opacity="1"/>
                                <TextBlock x:Name="SunIcon"
                                          Text="&#xE706;"
                                          FontFamily="Segoe MDL2 Assets"
                                          FontSize="10"
                                          Foreground="#E8A010"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Margin="5,0,0,0"
                                          Opacity="0"/>
                                <!-- Thumb (runder Knopf) -->
                                <Ellipse x:Name="Thumb"
                                         Width="16"
                                         Height="16"
                                         Fill="#E0E0E0"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         Margin="2,0,0,0">
                                    <Ellipse.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTranslate" X="0"/>
                                    </Ellipse.RenderTransform>
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="3" 
                                                          Opacity="0.25" 
                                                          Color="Black" 
                                                          ShadowDepth="1.5"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="MoonIcon" Property="Opacity" Value="0"/>
                            <Setter TargetName="SunIcon" Property="Opacity" Value="1"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource OnAnimation}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource OffAnimation}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
